"use client";

import type {NextPage} from "next";
import FAQContent from "@/app/(landing)/faqs/FAQContent";

const FAQ: NextPage = () => {

    return(
        <FAQContent />
    );
};

export default FAQ;
