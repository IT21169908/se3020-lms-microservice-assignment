/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'img.freepik.com',
            'res.cloudinary.com',
            'images.pexels.com',
        ],
    },
};

export default nextConfig;
