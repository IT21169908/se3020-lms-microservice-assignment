/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'img.freepik.com',
            'res.cloudinary.com',
            'images.pexels.com',
            'images.unsplash.com',
        ],
    },
};

export default nextConfig;
